# Build Issues Log — Smart Form to Adobe Form Migration

Every activation/runtime error hit on this repo, and its fix. Read before
concluding new code "should work." Append the day a new one is hit (Bolt
Playbook §0.4) — mirror the one-row summary into the Playbook's Appendix A too.

| # | Symptom | Cause | Fix |
|---|---|---|---|
| F1 | Dump `CALL_FUNCTION_CONFLICT_TYPE` (`CX_SY_DYN_CALL_ILLEGAL_TYPE`) calling `SSF_FUNCTION_MODULE_NAME` from `RESOLVE_FM_NAME`, passing `IV_FORMNAME TYPE string` directly to `FORMNAME` | `SSF_FUNCTION_MODULE_NAME` is a classic, pre-STRING-era function module — its `FORMNAME` parameter is a fixed-length `C`-like type, not `STRING`. `CALL FUNCTION`'s static type-check rejects a `STRING` actual there even though an ordinary `MOVE`/assignment would convert it fine | Declare a fixed `CHAR30` local, assign the string to it (`lv_formname = iv_formname.`), pass *that* to `FORMNAME` instead of the string variable directly (fixed in v0.4) |
| F3 | Activation error: *"The variable `<TAB>` must be escaped using `@`"* on `SELECT * FROM (lv_tab) INTO TABLE <tab> ...` in `PROBE_FORM_STORAGE` | Modern strict Open SQL requires **every** host variable — including an existing field symbol, not just an inline `DATA()` — to be prefixed with `@` when used as a SELECT target | `INTO TABLE @<tab>` (fixed in v1.5) |
| F4 | ATC warning: *"ABAP Doc comment is in the wrong position"* on the `"!` block preceding `TYPES: BEGIN OF ty_prog_info, ...` | A blank line before it wasn't enough (unlike the same warning on `PROBE_FM`'s `METHODS` doc, fixed by a blank line alone) — ABAP Doc appears not to support commenting a compound `TYPES:` chain declaring two types at once | Downgraded to a plain `"` comment (fixed in v1.5) — no functional loss, `"!` is only needed where a tool actually renders the doc (e.g. method signatures) |
| F2 | Activation errors in `EXTRACT_INCLUDES`: *"LV_INCLNAME must be a character-like field (data type C, N, D, or T)"* on the offset/length access, and *"LV_INCLNAME is not type-compatible with formal parameter IV_PROGNAME"* on the `write_driver_source( iv_progname = lv_inclname ... )` call | `DATA(lv_inclname) = to_upper( lt_words[ 2 ] ).` inferred `lv_inclname` as `TYPE string` (the return type of `to_upper( )`). Classic offset/length notation (`field+off(len)`) is **not valid on `STRING`**, only on fixed `C/N/D/T` types. Separately, `write_driver_source`'s `IV_PROGNAME` formal is `TYPE tadir-obj_name` (a by-reference `IMPORTING` param) — passing a `STRING` actual there fails type-compatibility even though a plain assignment would convert fine (same class of trap as ZAB_V1_UT engineering-log T2/T3) | Declare `lv_inclname` explicitly as `TYPE tadir-obj_name` (not inline `DATA()`), assign via `=` (plain assignment allows the conversion), *then* do the offset/length trim and pass it to `write_driver_source` — fixed in v0.9 |
| F5 | User: *"unable to view any of the files from the repo, abapGit is not able to read"* — all 4 `Z_MM_PR_FORM_ADF` deliverables (`.XDP`, `.XSD`, `SFPF_*.XML`, `SFPI_*.XML`) invisible to abapGit despite valid content | Files were placed under `docs/legacy_grab/` — this repo's own `.abapgit.xml` sets `STARTING_FOLDER=/src/` and explicitly `<IGNORE><item>/docs/*</item></IGNORE>`. abapGit was never looking there; correct XML content is irrelevant if the file sits outside the scanned folder. Self-inflicted: I authored that `.abapgit.xml` in v1.0 and didn't check it before placing new object-type files | Moved all 4 to `/src/` (fixed in v2.3). **General lesson, not just SFPF/SFPI-specific**: any file meant to become a real importable SAP object goes in `/src/`, never `docs/`, regardless of how unfamiliar the object type — check the repo's own `.abapgit.xml` `STARTING_FOLDER`/`IGNORE` before placing anything new, don't assume |
| F6 | User: *"still not able to clone through ABAPGit bcz of the format concerns"* — even after F5's folder fix, `SFPF_Z_MM_PR_FORM_ADF.XML` / `Z_MM_PR_FORM_ADF.XDP` / `SFPI_Z_MM_PR_FORM_ADF.XML` still would not clone | Wrong wrapper format, not a folder problem this time. I had modeled these files on the **SAP GUI "Utilities > Download" export** shape (`<asx:abap>` as the literal XML root, XDP base64-embedded inside `LAYOUTT/FPLAYOUTT/LAYOUT`). abapGit's real serializer for object types SFPF/SFPI (`LCL_OBJECT_SFPF`/`LCL_OBJECT_SFPI`) uses a **different, abapGit-specific shape**: an `<abapGit version="v1.0.0" serializer="LCL_OBJECT_..." serializer_version="v1.0.0">` root wrapping the `asx:abap` block, and — for SFPF only — the layout is **not** embedded at all; it ships as a **separate companion file** (`<name>.sfpf.xdp`, plain unencoded XDP) while `<name>.sfpf.xml`'s `LAYOUT href` points to a `CL_FP_LAYOUT` heap stub carrying only metadata. There is also **no standalone `.xsd` file** in the real format — the download export's `.XSD` was never a trackable abapGit object | Confirmed by having the user create a trivial Adobe Form in SAP and letting abapGit serialize it for real (`src/zhello_world_form_adt.sfpf.xdp`/`.sfpf.xml`/`zhello_world_adt.sfpi.xml`, pulled via `git pull`) — then rebuilding `Z_MM_PR_FORM`'s three files against that exact proven shape (v2.4/v3.0): dropped the standalone `.xsd`, split the XDP into its own `.sfpf.xdp` file, added the `<abapGit serializer="...">` wrapper to both XML files. **General lesson, not just this project**: when abapGit rejects a hand-authored serialization and the object type is unfamiliar, don't keep guessing at the wrapper shape from a raw SAP GUI download — get one real example of that *exact* object type serialized by abapGit itself (create the simplest possible object in the system, commit it, pull it) and copy its outer structure exactly; content facts (field names/types) can still come from other sources, but the wrapper shape cannot be reliably reverse-engineered from a non-abapGit export |

| F7 | Imported into SFP/LiveCycle Designer cleanly (Hierarchy tree showed every subform correctly), but Design View canvas rendered completely blank at any zoom level and with any node selected | The root template subform (`Z_MM_PR_FORM_ADT`) was declared `layout="tb"` (top-to-bottom auto-flow). Under `tb`, the layout engine **ignores every child subform's explicit `x`/`y`** and instead auto-stacks children by height in document order. The 9 body sections' heights sum to ~33cm, but the single `pageArea` was a fixed one-page area (default `occur` = exactly 1) only 21cm tall — the flowed content had nowhere valid to go, so nothing painted at all | Changed the root subform to `layout="position"` (XFA's own default when the attribute is omitted — confirmed by re-checking how `zhello_world_form_adt.sfpf.xdp`'s own body wrapper subform does it: no `layout` attribute, i.e. implicit `position`) so every child's explicit `x`/`y` is honored as designed. Also added `<occur min="0" max="-1"/>` to `pageArea` as a defensive multi-page allowance, matching the real `Z_ADT_MM_PR_FORM.XDP` reference's own pattern (fixed in v2.6). **General lesson**: `layout="tb"`/`"row"` and explicit per-child `x`/`y` are mutually exclusive — a subform meant to hold absolutely-positioned children (the norm when recreating a Smart Form's window-based layout) must be `layout="position"` (or omit `layout` entirely), never `"tb"` |

| F8 | After the F7 layout fix, Design View was *still* completely blank; the Context tab (Form Builder > Context) showed the Interface's Import/Tables parameters on the left but a completely empty tree under the form name on the right | Three compounding problems, found together: (1) every top-level `<bind ref="$.FIELDNAME"/>` used an invalid SOM prefix — `$.` is not valid XFA bind syntax; the only real bind ref in the confirmed reference (`Z_ADT_MM_PR_FORM.XDP`) uses `$record.`; (2) the watermark's `initialize` script referenced a bare `IV_FRGKZ` object that doesn't exist anywhere in the field hierarchy (only as a data node) — undefined-reference error at layout time; (3) the date-line's `initialize` script called `Date2Num`/`Mod`/`Num2Date` — FormCalc built-ins that don't exist in JavaScript — and referenced a bare `BADAT` instead of the actual field name `fld_badat`; both would throw at layout time. On top of the file-level bugs, the **Context tab is genuinely empty** — SFP's Context tree (what the Interface's parameters get exposed as inside the form's own data model) is a separate thing from the SFPF's `CL_FP_CONTEXT` heap node and is **not** something abapGit-imported XML populates on its own; it normally gets built by using SFP's own Context-tab tooling (a sync/generate action) after the Interface is defined, a manual SFP step this project hadn't done yet | Fixed the 3 script/binding bugs in `z_mm_pr_form_adt.sfpf.xdp` (v2.7): `$record.` prefix throughout, hidden `fld_frgkz` field so the watermark script has a real object to read, and the date-line script rewritten in plain JS with the real field name. **Still needs, as a manual SFP step**: use the Context tab's sync/generate control to populate the Context tree from the Interface's Import/Tables parameters before Design View can resolve any binding — this is not something a hand-authored abapGit XML file can pre-populate reliably (see the `CL_FP_CONTEXT` risk note already in `docs/reference_examples_z_adt_mm_pr_form/README.md`) |

| F9 | User dragged `T_FINAL` into SFP's Context tree (resolving F8's empty-Context problem) and got 11 activation errors: *"Reference field EBAN-MEINS of field T_FINAL-MENGE cannot be used here"* (and 10 more, same shape, naming `MARA-MEINS`/`EKKO-WAERS`/`EBAN-WAERS` etc.) | Standard ABAP Dictionary behavior, not a design defect: every QUAN (quantity) or CURR (currency) field needs a paired reference field carrying its unit-of-measure/currency key. `ZTABLE_PR_PRINT`'s fields inherited their *original* source table's reference (`EBAN-MEINS`, `MARA-MEINS`, `EKKO-WAERS`, `EBAN-WAERS` — copied in from wherever each field was originally sourced when the flat print structure was built) — those source tables aren't part of this Context, so the inherited reference can't resolve. Exactly the gap flagged (but deliberately left unpopulated) in `docs/reference_examples_z_adt_mm_pr_form/README.md`'s `CL_FP_REFERENCE_FIELDS` section — now confirmed live, not hypothetical | For each of the 11 QUAN/CURR fields, set its **Reference Field** property (in the Context node's Properties panel) to a field that actually exists in `T_FINAL`: `MENGE`/`MENGE1`/`BSMNG`/`MENGE2`/`LABST`/`LABST1` (QUAN) → `MEINS`; `NETPR`/`PREIS`/`EXCHANGE`/`SUPPLY_RATE` (CURR) → `WAERS`; `V_EXTTOTAL` (top-level, CURR) → `V_WAERS`. Chose these over the colleague's reference file's own (inconsistent — e.g. `LABST` self-referencing itself, `SUPPLY_RATE` pointing at a unit field instead of a currency one) `CL_FP_REFERENCE_FIELDS` values, which look like an unresolved wizard artifact rather than a template to copy. Added to `z_mm_pr_form_adt.sfpi.xml`'s `CL_FP_REFERENCE_FIELDS` (v2.8) so the fix survives the next re-import instead of being SFP-session-local |

| F10 | After F9's fix, applying `MEINS`/`WAERS` as the bare Reference Field value for the 10 `T_FINAL-*` QUAN/CURR entries produced 20 new activation errors: *"Field MEINS does not exist"* / *"Field WAERS does not exist"* for `Interface Z_MM_PR_FORM_ADT` | A reference field's `UNIT` value isn't automatically resolved as "the sibling column of the same name inside the table row" — it has to resolve to an actually-declared field *somewhere in the interface's own data model* (an import parameter, or a declared global). Confirmed by re-reading the reference file's raw `GLOBAL_DATA` (not previously extracted): it declares `MEINS TYPE EBAN-MEINS`, `LABST TYPE MARA-MEINS`, `NETPR TYPE EKKO-WAERS` as **dedicated standalone global variables**, purpose-built to be reference-field targets — they are not, and were never meant to be, the table row's own same-named columns. `V_EXTTOTAL`'s reference to `V_WAERS` (an actual existing import parameter) did NOT error, consistent with this — only the bare, undeclared `MEINS`/`WAERS` failed | Added two standalone `GLOBAL_DATA` entries to `z_mm_pr_form_adt.sfpi.xml`, matching the reference's exact pattern: `MEINS TYPE EBAN-MEINS`, `WAERS TYPE EKKO-WAERS`. Left the `REFERENCE_FIELDS` VALUE qualifiers as `T_FINAL-<field>` unchanged — SAP's own error text already addressed the fields that way, confirming that qualifier was correct all along; only the UNIT side was missing a real declaration. **Known follow-on gap, same class as other flagged Developer Extension Points**: nothing in this interface's `CL_FP_CODING`/`INITIALIZATION` (still empty) populates these two new globals with a real per-row value yet — same true of the reference file's own `MEINS`/`LABST`/`NETPR` globals, which also have no visible population logic. They exist to satisfy static reference-field validation; wiring a real value into them (from `T_FINAL`'s own row data, presumably via a FormCalc/JS calc script on the layout) is separate, follow-on work, not yet done |

| F11 | After F9/F10's Context-tab fixes, Design View was STILL completely blank - "not a single pixel changed" | User provided a THIRD real reference (`ZSD_SODETAILS_FORMS` - a Sales Order Details form), the first one small enough to read in full rather than in size-limited fragments. Full readability exposed 3 structural defects that fragment-reading had missed: (1) the root template `<subform>` must be named `"data"` (matching Hello World too) - ours was named `"Z_MM_PR_FORM_ADT"`; (2) every `<bind>` needs `match="dataRef"` alongside `ref="$.FIELDNAME"` - v2.7 had wrongly dropped the "$." prefix in favor of `$record.` based on one data point (a graphic-specific binding in the Z_ADT reference) instead of adding the actually-missing `match` attribute; (3) absolutely-positioned content subforms belong nested **directly inside `<pageArea>`**, not as top-level siblings of `<pageSet>` under a `layout="position"` root - the real reference's root is `layout="tb"` and every x/y-positioned subform sits inside `<pageArea>`, which supplies the fixed coordinate space | Rewrote `z_mm_pr_form_adt.sfpf.xdp` (v3.0): root subform renamed to `data` with `layout="tb"`/`restoreState="auto"`; every content subform moved inside `<pageArea>`; every `<bind>` corrected to `match="dataRef" ref="$.FIELDNAME"` (including table row bindings, previously bare unqualified names with no match attribute at all); non-data-bound layout subforms given explicit `<bind match="none"/>`; `xfa:datasets` simplified from a populated sample-data block to the proven-minimal `<xfa:data xfa:dataNode="dataGroup"/>` empty pattern, matching both known-good references exactly (removes a self-introduced, non-standard element that wasn't itself evidenced as necessary or safe) |

| F12 | Runtime dump `RAISE_EXCEPTION` / `TYPE_NOT_FOUND` running `ZSF2AF_R_LEGACY_GRAB`, terminating in `CL_ABAP_TYPEDESCR=============CM002` line 32, called from `PROBE_FORM_STORAGE` line 567 | `CL_ABAP_TYPEDESCR=>DESCRIBE_BY_NAME`'s `TYPE_NOT_FOUND` is declared as a **classic, non-class-based** exception on the method signature (`EXCEPTIONS type_not_found`), not a `CX_` exception. `TRY.  ... = cl_abap_typedescr=>describe_by_name( lv_tab ). CATCH cx_root.` (functional call + TRY/CATCH) can never catch it - TRY/CATCH only catches subclasses of `CX_ROOT`. Every one of the 4 speculative candidate table names (`STXFOBJECT`/`STXFATTR`/`STXFHEADER`/`SSFOBJ` - already flagged as "unverified, watch" below) that doesn't exist in this system dumps the whole program instead of being safely skipped - the "safe multi-table probing" design intent was never actually safe against this specific failure mode | Rewrote the call using classic `CALL METHOD cl_abap_typedescr=>describe_by_name ... EXCEPTIONS type_not_found = 1 OTHERS = 2.` + `sy-subrc` check, then a separate `TRY/CATCH cx_root` only around the `CAST cl_abap_structdescr(...)` (which genuinely does raise a `CX_` exception on a type mismatch). General lesson for any future ABAP in this project: a classic `EXCEPTIONS` parameter list on a method signature is a hazard sign - it means TRY/CATCH is not sufficient no matter how it's wrapped |

| F13 | After the v3.0 XDP rebuild, re-imported the form and Context is empty again - the T_FINAL drag-in and 11 Reference Field settings from the earlier session are gone | Our checked-in `z_mm_pr_form_adt.sfpf.xml` has a genuinely empty `CL_FP_CONTEXT` (a single root `CL_FP_NODE` with `<CHILD/>` empty) - it was deliberately left minimal in v2.1, matching Hello World's own empty Context, since we had no real reference to copy from at the time. Every abapGit import overwrites SAP's live copy of the object from our file, so *any* re-import (even one only touching the layout) resets Context back to empty, discarding whatever was built by hand in the tool since. Confirmed this isn't fixable by hand-authoring: the real reference's `CL_FP_CONTEXT` has 252 linked `CL_FP_NODE`-based objects (`CL_FP_FOLDER`/`CL_FP_DATA`/`CL_FP_LOOP`/`CL_FP_LOOP_DATA`/`CL_FP_CONDITION`/`CL_FP_ALTERNATIVE`, cross-referenced by GUID-like IDs via `PARENT`/`SUCCESSOR`/`CHILD`) - a genuine SAP kernel serialization output, not something meant to be hand-typed. Attempting to fabricate an analogous tree by pattern-matching risks a subtly-corrupted Context that's harder to diagnose than an empty one | **Workflow fix, not a code fix**: Context is a *build-once-in-the-tool, then pull-to-capture* artifact, not a push-from-repo one. Plan: (1) rebuild Context one more time in SFP (drag `T_FINAL` in, set the 11 Reference Fields, exactly as before); (2) check Design View/Layout *before* doing anything else, now that Context has real data and the v3.0 structural fixes are in place; (3) once it renders, **pull** (not push) the object via abapGit so the repo captures SAP's real, tool-built Context, replacing the empty stub for good. From that point, avoid re-pushing over a captured Context unless intentionally rebuilding it. General lesson for all future forms: don't leave Context empty and hope SFP auto-generates it - either capture a genuinely tool-built one via pull, or budget the manual drag-in step as a real, repeatable part of every import cycle until it's captured |

| F14 | After rebuilding Context and setting the 11 F9/F10 reference fields, one error remained: *"Reference field EBAN-WAERS of field T_FINAL-TOTAL cannot be used here"* (plus 8 non-blocking warnings) | `TOTAL` is also a CURR field in `ZTABLE_PR_PRINT` needing a reference field - missed in F9's list because the colleague's reference file's own `CL_FP_REFERENCE_FIELDS` didn't include an entry for it either (that reference's list isn't necessarily exhaustive of every CURR field in the real structure - live system feedback is more reliable than that file for this specific question) | Added a 12th entry: `T_FINAL-TOTAL` (CURR) → `WAERS`, same pattern as `NETPR`/`PREIS`/`EXCHANGE`/`SUPPLY_RATE`. Error count 1 -> expected 0 |

| F15 | After v3.0 (content nested inside `<pageArea>`) plus F9/F10/F13/F14's Context and reference-field fixes, activation reached 0 errors but Design View, and separately Print Preview, both still showed nothing at all - not even a page | v3.0's pageArea-nesting fix was reasoned from `ZSD_SODETAILS_FORMS`, a reference **never actually confirmed to render in this tool/environment** - only assumed because it's a real file. Re-checked against `zhello_world_form_adt.sfpf.xdp`, the ONE file the user has directly confirmed renders in this exact SFP session: its `<pageArea>` contains ONLY `<contentArea>`/`<medium>` - no content at all. Its actual content sits in a **separate subform, a sibling of `<pageSet>`** (not nested inside it), with no explicit `layout` (defaults to `"position"`), sized to the full page. The `"tb"` root flows that one subform (landing it at the top of the page, full-page-sized); everything inside it then places by its own x/y because that subform's own layout is `"position"`. Two different real files suggested two different structural conventions - only one of them was actually verified to work in this environment, and it wasn't the one v3.0 followed | Reverted the pageArea-nesting: `<pageArea>` now holds only geometry again; all 9 content subforms (`logo`/`header`/`type_of_request`/`pr_header`/`value_line`/`watermark`/`date_line`/`page_footer`/`main`) moved into one new `page_body` subform (`layout="position"`, sized to the full page), sitting after `</pageSet>` as body content - matching Hello World's confirmed pattern exactly. Kept v3.0's other two fixes (root named `data`, `match="dataRef"` on every bind), which remain independently correct and orthogonal to this question. **General lesson**: when two real references disagree, only trust the one *directly confirmed to render in the user's own environment* - "it's a real file" is not the same evidence as "it's been seen working" |

| F16 | After v3.1's revert (matching Hello World's exact skeleton), Design View was STILL blank - and a direct UI test (dragging a brand-new Text field from the Object Library onto the empty canvas) also showed nothing. The object then started showing as corrupted/unresponsive - "not able to make any changes, layout corrupted" | The direct-UI-test result is the key fact: since even a NEW element placed via the tool's own UI (not from our imported XML at all) didn't appear, the problem was not conclusively isolated to our file's content - it could equally be the object's own state, corrupted by repeated import/edit/activate cycles across v2.0-v3.1. Continuing to guess at the full design's structure was the wrong move at this point | **Reset, not another guess.** (1) Delete both `Z_MM_PR_FORM_ADT` objects (form + interface) in SFP and recreate fresh, ruling out corrupted object state as a variable. (2) Stripped `z_mm_pr_form_adt.sfpf.xdp` down to a deliberate minimal baseline (v4.0): root "data" -> pageSet/pageArea (geometry only) -> one `page_body` subform -> one static draw + one bound field (`BANFN`) - as close to Hello World's exact proven shape as possible while still being our real object. No image, table, scripts, or HTML blocks. The full design (preserved in git history at commit `0b8e141`/v3.1 and in the build checklist) gets added back **one piece at a time**, confirming Design View after each addition, only once this baseline is confirmed rendering. General lesson: when repeated whole-file rewrites all fail, stop rewriting the whole file - fall back to the smallest possible working baseline and rebuild incrementally, testing at every step, rather than guessing at the full structure again |

| F17 | Even the v4.0 minimal baseline (one draw + one field) showed a blank Design View, and the Layout screen became fully unresponsive - can't select, delete, or add any element, on either the fresh minimal file or by direct UI interaction | Isolated systematically rather than guessed: (1) a parallel test object built from the real `Z_ADT_MM_PR_FORM` reference hit its own unrelated DDIC error (`Field MEINS does not have data type CUKY`) blocking its Layout entirely - a dead end, abandoned; (2) tested whether the *environment* was broken by fully closing SAP GUI and logging back in fresh, then re-checking `ZHELLO_WORLD_FORM_ADT` - it rendered fine, ruling out a session/tool/plugin-wide problem; (3) reopened `Z_MM_PR_FORM_ADT` in that same fresh session - still stuck/unresponsive. Conclusion: **the object itself is corrupted**, most likely from being repeatedly re-imported/edited/activated across v2.0 through v4.0 (roughly a dozen import cycles) - SFP forms carry a generated/cached runtime representation separate from the XDP source, and abapGit re-import only replaces the source, never clears that cache. Confirmed the design content itself was never the remaining blocker | **Delete the object for real, don't just re-import over it.** In SFP, delete both `Z_MM_PR_FORM_ADT` (form) and `Z_MM_PR_FORM_ADT` (interface) via the transaction's own Delete function (not just overwriting via abapGit) - this clears whatever generated/cached state a plain re-import can't touch. Then do a fresh abapGit pull to recreate both objects cleanly from the current v4.0 minimal source, and test Layout on the newly-created object before adding anything back. **Standing lesson for all future forms**: if a form object starts showing UI-level corruption (unresponsive Layout, can't select/edit) after several import cycles, delete-and-recreate immediately rather than continuing to debug in place - don't wait as long as this pilot did (a dozen cycles) before reaching for it |

| F18 | abapGit pull failed: *"PREFIX: Unexpected package naming (top: ZABAP_UTIL, parent: ZABAP_UTIL, child: ZFDP_PREVIEWER). Try using the folder logic FULL"* | A new, intentionally-created package (`ZFDP_PREVIEWER`, part of this project) had been created as a direct child of the shared `ZABAP_UTIL` package without a name matching `FOLDER_LOGIC=PREFIX`'s requirement (child package names must start with the parent's name, i.e. `ZABAP_UTIL_*`). Moving the package's objects into a correctly-named package wasn't feasible (already transported/in use elsewhere); an abapGit "exclude subpackages" repo setting doesn't exist/isn't editable post-creation in this abapGit version | Reparented `ZFDP_PREVIEWER` itself (SE21, Superpackage field) away from `ZABAP_UTIL` - lighter than moving its objects, since it only changes the package hierarchy pointer, not the objects' package assignment or transport history. Once it's no longer a child of `ZABAP_UTIL`, this repo (scoped to `ZABAP_UTIL`) doesn't discover it at all, and `FOLDER_LOGIC=PREFIX` + the flat `/src/` layout stay untouched. **General lesson given `ZABAP_UTIL` is shared across 4 repos** (this one, VS-Tower, Dangote_Requirements, ZAB_V1_UT): any new package created under a shared parent package needs PREFIX-compliant naming from the start, or it will break abapGit pulls for every repo scoped to that parent - worth a shared Bolt Playbook entry if this recurs |

| F19 | Same class of error as F18, on a different package: *"PREFIX: Unexpected package naming ... child: ZFDP_SCRATCH"* | A second `ZFDP_*` child package under `ZABAP_UTIL`, this one empty/junk. Confirmed only 2 occurrences so far, not (yet) a per-session pattern from Layout/Print Preview testing - worth watching for recurrence, but not treated as systemic yet | Since it was empty, deleted the package outright (SE21) rather than reparenting - simpler and permanent when there's genuinely nothing to preserve. If a third `ZFDP_*` package appears, treat it as a confirmed systemic pattern (likely an SFP/ADS default-package setting worth raising with Basis) rather than another one-off fix |

| F20 | A newly created, activated SFP form showed a normal physical page and native `User` field, but an abapGit Pull reverted it to `Failure to layout form: no pageSet element defined` | Pull imports the repository artifact into SAP and overwrote the working SFP-generated layout with the prior hand-authored form | After a form renders in SFP, use abapGit **Stage → Commit → Push** to export it. Do not Pull over a newly working form |

| F21 | The old pilot artifacts could overwrite the healthy SFP form before its standard serialization was captured | The tracked layout and metadata were hand-authored rather than produced by SFP | Removed them temporarily; the user then pushed the healthy SFP-generated artifact set |

| F22 | The clean SFP-generated pilot baseline was pushed at `c507878` | Its XDP has a real `pageSet`/page area and its SFPF contains the generated Context graph | Preserve the SFPF/SFPI Context artifacts. Design only the XDP in small rendered increments, then **Stage → Commit → Push** each one |
| F23 | The initial repository-side Adobe layout showed no usable Design View, while a form created and activated directly in SFP rendered normally; after its repository capture, the first visual-shell increment (`bf5d290`) also rendered successfully in the user's SFP session | The failure was **not** ADS availability, the logo URL, or the static design elements. An abapGit **Pull** imports repository files into SAP, so it had replaced the healthy SFP-generated form—including its valid `pageSet` and generated Context—with earlier hand-authored serialization. Repeatedly importing alternative hand-authored forms compounded the object corruption; XML well-formedness alone never establishes SFP/Designer renderability | Treat the first SFP-created, rendered, activated, and **Stage → Commit → Push**-captured object as the immutable serialization baseline. Preserve `.sfpf.xml` Context and `.sfpi.xml` unless a change is made in SFP itself. Make XDP-only changes in small increments, Pull only a known-good published increment into a clean SAP object, validate **Layout + activation**, then capture SFP's result with **Stage → Commit → Push** before the next increment. The user confirmed the `bf5d290` visual-shell render on 2026-09-12 |
| F24 | The 290 mm line-item table and right-side PR header overlapped or ran off the visible page even though the XDP declared `long="297mm"` and `short="210mm"` | The master-page `<medium>` omitted the XFA `orientation="landscape"` attribute. LiveCycle therefore rendered the A4 page at 210 mm wide (portrait); all legacy coordinates and the 290 mm table were correctly sized for 297 mm landscape width, making the apparent overlap inevitable. The content area was also only 285 mm wide, while the captured 290 mm table begins at x=5 mm | Set `<medium long="297mm" orientation="landscape" short="210mm" stock="a4"/>` and use a 290 mm content area at x=5 mm, matching the table's 295 mm right edge within the 297 mm page. For every landscape Smart Form, test the physical Design View width before adding controls: page width must be at least the rightmost captured window edge, and the sum of table columns must fit within the content area |
| F25 | A newly created form such as `YMMGRNNOTE_ADT` remains inactive with a blank Design View and no mapped nodes in Form Builder Context after a repository pull | A baseline XDP/SFPI shell—even one that is syntactically valid and has exact parameter records—does not create the form's linked `CL_FP_CONTEXT` node graph. The left Interface tree and right Context tree are distinct; interface parameters remain unmapped until a user places them in Context. An inactive interface/form and unresolved DDIC QUAN/CURR reference fields prevent usable Context mapping. Hand-authoring the graph or guessing component references is unsafe | Pull the exact SFPI, add legacy exceptions through SFP, inspect each generated table/structure component and assign every QUAN/CURR its real DDIC unit/currency reference in the Context Properties UI, activate the interface and form, then manually drag/drop required parameter/table nodes from the left Interface tree into the right Context tree. Confirm the mapped nodes appear and use abapGit **Stage -> Commit -> Push** to capture the SAP-generated SFPF/SFPI before any bound XDP layout is introduced. Applies to every conversion branch; correct the already-promoted F25 wording on `main`. |
| F26 | An active target SFP interface remained blank after Pull although manually authored `SFPIOPAR` records matched the XML shape of an archived generated SFPI example | XML element-shape similarity is not proof that manually authored interface records are accepted, activated, or surfaced by the target SFP release/object state. The form-specific capture had no successful SAP-side import evidence, so treating the repository records as a populated interface was unsafe | Revert the unvalidated parameter records to the SFP-generated empty envelope. Enter the exact evidence ledger natively in SFP, activate, verify visible parameters, map Context manually, then **Stage -> Commit -> Push** the target system's generated SFPI/SFPF. Do not reuse handwritten parameter/global/code records as a baseline. Reusable finding; promote to `main`. |

| F27 | The interface scope ledger described every `%CODE` node's evidence conceptually (which fields, which DEP) but not the exact ABAP text, leaving no way to confirm whether a Developer Extension Point could be resolved without re-deriving the source | The condensed report format (`ZSF2AF_R_LEGACY_GRAB`'s output) doesn't capture literal Program Lines text - only the raw immutable `ymmgrnnote.xml` download does, as `<sf:CODE><CODE><item>...</item></CODE>` arrays keyed by `%CODEn` name, findable by byte-offset search since the file is a single 209,891-byte line | Extracted the complete, literal source for the global initialization and all five `%CODE` nodes directly from `ymmgrnnote.xml` by byte offset and added it verbatim to `ymmgrnnote_interface_scope_ledger.md`. Surfaced two findings only visible at this precision: (1) `%CODE1`/`%CODE3` run **per `LT_MSEG` row** (using loop work area `WA_MSEG`) while Adobe's `CL_FP_CODING`/`INITIALIZATION` only runs once - a real design decision (FormCalc per-row vs. an ABAP loop in INITIALIZATION), not free evidence; (2) the footer's computed `DATE` (from `BLDAT` via a month-name FM) and the `DATE` window's raw `BUDAT` are two different fields that look similar in the condensed evidence. General lesson: for any form with real embedded logic, pull the literal code text before writing the interface coding spec - a conceptual paraphrase can hide execution-scope and field-identity distinctions that only the exact source reveals |

| F28 | User explicitly requested re-attempting hand-authored `SFPIOPAR`/`SFPGDATA` records for `YMMGRNNOTE_INT` and a Pull, after F26 already documented this exact approach failing (interface stayed blank after Pull). Result this time: **`SFPI error, deserialize. Import of object YMMGRNNOTE_INT failed`** - a hard error, not silent blankness | Repeated at explicit user instruction, after stating the F26 precedent directly - not a silent repeat of a known-bad pattern. This attempt populated more than F26 did (`IMPORT_PARAMETERS`, `EXPORT_PARAMETERS`, `TABLE_PARAMETERS`, `EXCEPTIONS`, `GLOBAL_DATA`, `TYPES`) and got a *worse*, harder failure - a genuine deserialize exception rather than an accepted-but-inert import. The two sections added with no confirmed real reference (`EXCEPTIONS`'s `SFPIOPAR` shape, `TYPES`'s `SFPGDATA` shape - only `IMPORT_PARAMETERS`/`GLOBAL_DATA` were evidence-backed from the pilot's own file) are the most likely structural cause, though the generic error text doesn't confirm which element specifically | Reverted `ymmgrnnote_int.sfpi.xml` to the exact safe empty baseline (verified byte-identical to commit `310a498`) rather than debug the schema further. **This is now the second, more severe failure of hand-authoring this object type on this SAP release** - treat native SFP entry (S01) as the only remaining viable path for `CL_FP_PARAMETERS`/`CL_FP_GLOBAL_DEFINITIONS` on this system, not a preference. Also worth checking: did the failed deserialize leave the live SAP object in a partially-modified state (matching the F17 corruption pattern)? Confirm the interface still opens/behaves normally before typing anything into it natively |

| F29 | User natively entered `LS_MKPF` (from the F28-recovery isolation test), a new global `LV_MBLNR`, and a test `INITIALIZATION` line in SFP, then captured it with abapGit Stage -> Commit -> Push (`32d66b6`) | This is the first genuinely SFP-generated, non-hand-authored capture of `IMPORT_PARAMETERS`, `GLOBAL_DATA`, and `CL_FP_CODING` content for this object - confirmed by its distinct multi-line indented XML formatting (abapGit's own serializer output), unlike any of this project's hand-authored attempts | Proves the full native-entry-then-capture pipeline works end to end for this object, and confirms `IMPORT_PARAMETERS`/`GLOBAL_DATA`/`CL_FP_CODING`'s schema shapes are exactly what was guessed in F28 - the F28 failure is now most likely isolated to `EXCEPTIONS` and/or `TYPES` specifically, not the whole hand-authoring approach. Given this stronger evidence base, filled in the complete remaining interface (19 more imports, 3 exports, `LT_MSEG` table, 4 exceptions, 14 more globals, 1 type, and the real global-initialization/`%CODE5`/`%CODE2` logic) by hand, matching the now-confirmed multi-line format exactly, at the user's explicit request to avoid further manual SFP entry. `EXCEPTIONS`'s and `TYPES`'s exact shapes remain the two least-confirmed parts - if this fails, look there first. **DEP-YMMGRNNOTE-01's authorization question (reading another user's name via USR21/ADRP off a cross-program MEMORY ID) is included in this push as literal, unreviewed legacy code** - flagged again here for the record, not blocking, since the user directed this content be entered in full |

| F30 | Isolation test (`617614d`: same 20 imports/3 exports/`LT_MSEG`/15 globals/real init code, `EXCEPTIONS` and `TYPES` emptied) deserialized and activated cleanly - `YMMGRNNOTE_ADT` shows Active, Interface tree populated with Import/Tables/Global Data | Confirms definitively: `IMPORT_PARAMETERS`, `EXPORT_PARAMETERS`, `TABLE_PARAMETERS`, `GLOBAL_DATA`, and `CL_FP_CODING` (including `INPUT_PARAMETERS`/`OUTPUT_PARAMETERS`/`INITIALIZATION`) can be safely hand-authored and captured via Pull, once their schema is confirmed from a real SFP-generated sample. `EXCEPTIONS` and `TYPES` were the specific, isolated cause of F28/F29's deserialize failures - not hand-authoring in general | **Promoted to `docs/strategy/S04_interface_hand_authoring_boundary.md`** as the standing rule for every future form's interface: hand-author parameters/globals/coding directly (fast, git-manageable), but leave `EXCEPTIONS` and `TYPES` for native SFP entry, and leave `CL_FP_CONTEXT`/Context mapping native regardless (a linked GUID graph, not a flat list - a different class of risk, per F13 on the pilot) |

| F31 | After dragging `LS_MKPF`/`LS_MSEG`/`LT_MSEG`/`WA_MSEG`/`LS_EBAN`/`LS_EKKO` into Context, 27 activation errors: every `QUAN`/`CURR` field's inherited default reference pointed at a table not in Context (`LIPS-GEWEI`, `BSEG-MEINS`, `RV61A-KOEI1`, `MSEG-WAERS`, `MARA-MEINS`, `WRF_POTB_CURRENCY_STY-OTB_CURR`) | Same class as the pilot's F9/F10 - live evidence, not documentation, revealed the exact real field list for the first time: `ZEXTRA_FIELD` (via `LS_MSEG`/`LT_MSEG`/`WA_MSEG`) carries `PBAMG` (QUAN) and `VAT`/`FRI`/`OTH`/`DIS`/`LV_URATE` (CURR) - the last three (`FRI`/`OTH`/`DIS`) previously only seen as commented-out in `%CODE3`, now confirmed real fields. The customer-appended `MKPF` (via `LS_MKPF`) carries `BRGEW`/`TARE_WT`/`NTGEW`/`ZZACTUAL_DDS_MENGE`, all weight-type. Standard `EBAN`/`EKKO` fields (`MNG02`/`BSMNG_SND`, `OTB_VALUE`/`OTB_RES_VALUE`/`OTB_SPEC_VALUE`) also needed resolving | Populated `CL_FP_REFERENCE_FIELDS` with all 27 entries, hand-authored and pushed directly (this section is a flat list, not the Context tree - already proven safe on the pilot form). Pointed each at the most natural in-context field: `ERFME` (a real `ZEXTRA_FIELD` component) for the QUAN row fields; the interface's own `IV_WAERS` for the CURR row fields; `LS_EBAN-MEINS`/`LS_EKKO-WAERS` (real native fields on those full structures) for the EBAN/EKKO fields; a new standalone global `GV_GEWEI TYPE GEWEI` for the four weight fields, since no evidenced weight-unit field exists elsewhere in scope |

| F32 | After F31's fix, 3 remaining errors: "Field ERFME does not exist" for the three `PBAMG` reference entries | Same lesson as the pilot's F10: a reference field's `UNIT` must resolve to an actually-declared, uniquely-addressable field - a bare name doesn't auto-resolve to a sibling component of the same structure as `VALUE`. `LS_EBAN-MEINS`/`LS_EKKO-WAERS` (already prefix-qualified) didn't error in F31, confirming the pattern | Qualified all three: `ERFME` -> `LS_MSEG-ERFME`, `LT_MSEG-ERFME`, `WA_MSEG-ERFME` respectively, matching each entry's own `VALUE` prefix |

| F33 | Milestone, not a bug: `YMMGRNNOTE_ADT`'s Context was fully built natively and captured via Stage -> Commit -> Push (`17bd45c`) - 3,420 real `CL_FP_NODE` entries, 6,788 `CL_FP_DATA` fields, well-formed | First form on this branch to reach the "Build baseline" gate with a genuinely SFP-generated, non-empty Context. Design View also renders real content ("Goods Reciept Note") for the first time on this form | Began the first XDP increment on top of it: A4 portrait page geometry (was Letter, SFP's own default - no evidence either way, but matches every other Dangote form's convention), the SE78 logo, and exactly one bound field (`LS_MKPF.MBLNR`) as a live test of the `$.<structure>.<field>` addressing convention before binding anything else. The captured Context showed both a bare `BUDAT` and a `BUDAT_MKPF` at different tree positions - not yet resolved which one this form's own `LS_MKPF-BUDAT` maps to; deliberately not guessing at that binding yet |

| F34 | Continuing the v1 increment: header identity, GRN date, and the full `TEMPLATE` window's 17 fields needed exact field-to-label mappings the condensed legacy-grab evidence didn't have (only listed which structures the window draws from, not which field goes with which label) | Byte-offset extracted `%TEXT3`/`%TEXT4` (the `TEMPLATE` window's two columns, widths 10.66cm/9.54cm matching the checklist's evidenced 20.20cm total) directly from `ymmgrnnote.xml`, the same technique already proven for the `%CODE` nodes and conditions | Built the company/plant identity block using real-data fields (`LV_BUTXT`, `LS_T001W.NAME1/STRAS/PFACH/ORT01`) rather than the still-unapproved 5-way conditional branding (`DEP-YMMGRNNOTE-05`) - matches checklist item 6's explicit instruction to keep that conditional logic annotated, not implemented, until approved. Built all 17 `TEMPLATE` fields with their exact evidenced bindings, preserving legacy typos verbatim (`WAY_BII_NO`, `Loding`) rather than silently correcting them, and flagged `LS_EBAN.MFRPN` (Manufacturer Part Number) being used for "Department Name" as worth a functional-owner sanity check rather than assumed correct. The repeating `MAIN` table and footer/signature block are deliberately deferred to the next increment - a different binding pattern (`occur`/loop) than anything tested on this form yet |

| F35 | `template_col1`/`template_col2` rendered as garbled overlapping text instead of side-by-side columns; warning triangles appeared right at their shared boundary | `template_block` used `layout="row"` to place two subform-columns side by side, where each column was itself a stack of row-subforms (`row` containing `tb` containing `row`). This 3-level mixed-layout nesting didn't compute reliably in this Designer/rendering engine, even though row layout with 2 leaf children (proven working elsewhere in this same file, e.g. `grn_no_line`) is fine | Replaced `layout="row"` with `layout="position"` on `template_block`, giving `template_col1`/`template_col2` explicit `x` offsets (`0cm`/`10.66cm`) instead of relying on row auto-flow - matching the pilot's own proven pattern of explicit positioning for anything side-by-side, rather than auto-flow layouts for non-trivial nested content. Applied the same defensive fix to `company_identity` (which mixed plain fields with a nested `row` subform under `tb`), converting it to `position` with explicit `y` offsets too, even though it wasn't confirmed broken - same risk shape, same fix. **General lesson**: `row`/`tb` auto-flow is reliable for a flat set of leaf (field/draw) children, but not proven reliable once a row/column itself contains further nested subforms of differing internal structure - prefer explicit `position` + coordinates for anything past that complexity, matching the pilot's own convention throughout |

## Unverified table names to watch (not yet confirmed traps)

`global_sweep` (P_GLOB, v1.2) reads `STXBITMAPS` for SE78-registered
graphics via a **static** `SELECT *` — this table name has not been used
anywhere else in this project and is a best-effort guess. If it's wrong,
expect an **activation-time** error naming the table (not a runtime dump,
since `SELECT *` needs no field names) — paste it back and it's a one-line
fix (swap the table name).

`probe_form_storage` (v1.4) reads four other unverified candidate tables —
`STXFOBJECT`, `STXFATTR`, `STXFHEADER`, `SSFOBJ` — but via **dynamic**
`SELECT * FROM (lv_tab)` inside `TRY...CATCH cx_root`, resolved through
`cl_abap_typedescr=>describe_by_name` first. A wrong name here is a caught
runtime exception, not an activation failure — genuinely zero risk to the
program even if all four don't exist (which is plausible; they're
speculative). No fix-round needed if they all miss — the snapshot just says
so and points at the debugger-based alternative
(`docs/02_legacy_grab_spec.md`).

## Lesson for future calls in this project

Any **classic** (pre-Unicode-era) function module — `SSF_*`, most `SAPscript`/
Smart Forms APIs, many old BAPIs — is likely to have fixed-length `C`/`N`/`D`/`T`
typed parameters, not `STRING`. Before passing a `STRING`-typed variable into
one, convert it to a fixed-length local first. Modern function modules
(`GUI_DOWNLOAD` and most post-7.0 APIs) generally accept `STRING` natively —
but if a new `CALL_FUNCTION_CONFLICT_TYPE` dump shows up anywhere else in this
report, the fix is the same pattern: add a fixed-length local, assign, pass
that instead of the raw `string`.
